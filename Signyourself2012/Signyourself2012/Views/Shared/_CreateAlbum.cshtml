@{
    Html.ActionLink(
        "Create Album", "Create", "Albums"
        ,new ViewDataDictionary{
                                {"UserId", ViewBag["UserId"]}, 
                                {"CampaignId", ViewBag["CampaignId"]} 
                             }
        ,new {@class = "sm_button"}
  );
}

